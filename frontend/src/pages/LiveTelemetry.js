import React from 'react';
import { Typography, Box } from '@mui/material';

const LiveTelemetry = () => {
  return (
    <Box>
      <Typography variant="h4">Telemetria Live</Typography>
      <Typography>Pagina in sviluppo...</Typography>
    </Box>
  );
};

export default LiveTelemetry;
