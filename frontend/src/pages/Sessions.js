import React from 'react';
import { Typography, Box } from '@mui/material';

const Sessions = () => {
  return (
    <Box>
      <Typography variant="h4">Sessioni</Typography>
      <Typography>Pagina in sviluppo...</Typography>
    </Box>
  );
};

export default Sessions;
