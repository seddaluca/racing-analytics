import React from 'react';
import { Typography, Box } from '@mui/material';

const Analytics = () => {
  return (
    <Box>
      <Typography variant="h4">Analytics</Typography>
      <Typography>Pagina in sviluppo...</Typography>
    </Box>
  );
};

export default Analytics;
