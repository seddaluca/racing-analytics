import React from 'react';
import { Typography, Box } from '@mui/material';

const Comparison = () => {
  return (
    <Box>
      <Typography variant="h4">Confronti</Typography>
      <Typography>Pagina in sviluppo...</Typography>
    </Box>
  );
};

export default Comparison;
